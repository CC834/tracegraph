export function App() {
  return (
    <main className="app-shell">
      <header>
        <strong>TraceGraph</strong>
        <span>Read-only relational investigation</span>
      </header>
      <section>
        <h1>Record lineage explorer</h1>
        <p>The frontend workspace is ready for trace configuration and graph exploration.</p>
      </section>
    </main>
  )
}
